difficulty: hard
agent: inherit

# 生成式填充和扩图

## T1 · 真实生成、区域保真与工程恢复

前置依赖：09-smart-cutout-erase.md，以及 08 评估确定的可用真实生成后端。凭据、模型或资源缺失时记录 blocker。

要做什么：在既有后端和 ops 体系加入 fill/outpaint，明确提示词、区域/蒙版、画布扩展、输出尺寸、支持的 seed/生成参数，执行前拒绝不支持参数。必要时将模型输出和受保护区域按明确策略合成，保留区域外原像素；记录真实生成结果资产和全部执行元数据。重放/撤销不能重新生成；继续编辑或输入改变须正确失效。

预计修改：pic-core 智能后端、operation/document/pipeline、pic-cli、接入文档与测试，本 todo/README。

验收条件：

- [ ] fill 和 outpaint 从真实输入/掩膜/提示词经实际模型产生有效输出；尺寸、扩图边界、区域保真和任务效果有可查看证据。
- [ ] 参数能力、缺配置、模型错误/超时、格式/区域不匹配准确返回，不把接收任务当成功完成。
- [ ] 已提交结果可在模型离线时跨进程完全恢复；模型资产/seed/版本等元数据持久化，修改输入不误命中。
- [ ] 前后历史、局部预览与坐标映射、全尺寸导出正确，完整仓库校验通过。

验证：完整仓库校验；真实生成样例与区域外像素检查、离线恢复、失败及 stale revision 测试。不得以 mock 代替效果验收，也不要求不同模型输出逐像素相同。
