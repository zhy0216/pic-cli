difficulty: hard
agent: inherit

# 普通编辑与工程性能

## T1 · 建立端到端基准并按证据优化

前置依赖：07-groups-text-adjustment.md。本轮仅测试无需模型的直接编辑。

要做什么：建立可复现基准素材与脚本，登记 CPU/内存/OS/磁盘/线程/编译器/release 设置；至少 30 次新进程采样报告 p50/p95 与峰值内存、阶段耗时。覆盖 --version、1080p JPEG 缩放曝光饱和度、4K 调整、PNG 透明图、4K 多图层和大模糊；工程首次提交、清缓存重放、磁盘检查点恢复、重复预览、20 步修改第 18/3 步的耗时/重算/磁盘占用。区分 OS 文件缓存和工程缓存，不能伪称冷缓存。根据实测选择减少拷贝、等价点操作融合、缩放/编码和线程预算、检查点/缓存策略改进；不牺牲语义精度/采样/编码质量。

预计修改：基准脚本/素材生成与报告、pic-core 中经测量确认热点、必要 CLI 计时与文档/测试，本 todo/README。

验收条件：

- [x] 所有普通编辑和工程场景均有 release 二进制端到端 30 次测量、p50/p95、运行条件和可重跑命令。
- [x] 报告相对建议门槛的实际结果与未达标原因；无未经测量的加速承诺，libvips 未可用则如实说明对照限制。
- [x] 优化有前后测量及解码像素/语义一致性证据；操作融合后任意逻辑步骤仍可观察。
- [x] 线程/内存/磁盘预算可说明，不依赖降低质量；建立可运行的性能回归检查方法并避免不稳定 CI 门槛。
- [x] 完整仓库校验通过。

验证：完整仓库校验；固定 release 基准至少 30 轮；像素/重放/检查点一致性回归；输出原始机器可读结果和简洁分析。


## 完成证据（2026-09-19）

- 全部普通编辑及工程场景：固定 release 二进制前后各 16×30=480 次成功新进程采样，每场景另预热一次；包含真实双层 4K、73-tap 大模糊、首提交、清工程缓存重放、磁盘检查点、重复预览和独立复位的第 18/3 步修改。逐次 wall、CLI 阶段、单 CLI RSS、真实缓存命中、重算 revision、磁盘占用和负载均入库。
- 目标与限制：after p95 version 3.78 ms、1080p 252.64 ms 达到建议目标；4K 调整 1171.49 ms 仍高于 1 秒，已报告编码/处理阶段原因。libvips 命令不可用，未声明跨后端优势；OS 页缓存未清理，工程 cache-clear 不称冷磁盘。
- 有依据的优化：8-bit sRGB 以原公式生成完整 256 项查表；restore 复用管线/完整检查点已有画布，避免重复合成。4K 重放/检查点恢复/检查点写入 p95 分别下降 33.8%/30.0%/39.1%。完整中间步骤保留，没有操作融合、精度/采样/编码质量改变。
- 一致性：45 组解码比较、574,387,200 个通道零差异，r0–r20 文档/历史一致；修改第 18/3 步、完整重放及读取旧版多层检查点一致。新增全字节 f32 bit 与逐前缀/分块/缓存层回归通过。
- 预算与回归：一个 CLI、CPU affinity=[0]、观察到一个线程；默认 1 GiB 工作预算与 512/256 MiB 缓存预算不变。双层 4K 默认 disk_stored=false 如实记录，命中场景显式使用 1 GiB 缓存内存并断言 disk/checkpoint 或 disk/preview。峰值约 801 MiB，未承诺减少 RSS。回归脚本默认只报告，时间阈值为受控同机可选项。
- 完整校验：fmt、clippy -D warnings、workspace tests（129）、release build、diff check 全通过；额外 release 核心测试 80 项、release CLI 集成 49 项通过。

分析、运行条件、重跑命令及逐条证据索引：[docs/performance.md](../../../../docs/performance.md)。原始样本/统计/像素证据/校验日志均在 [docs/performance/](../../../../docs/performance/) 跟踪保存，二进制和大图工程仅为可重建临时产物。未修改 plan.md 或其他任务状态，未 push/PR/rebase/merge，等待协调器集成。
