difficulty: hard
agent: inherit

# 抠图、主体分割和对象移除

## T1 · 接入真实模型并保存可恢复结果

前置依赖：07-groups-text-adjustment.md、08-smart-backend-evaluation.md；需要对应可用真实模型后端。后端确实无法获得时记录 blocker，不能用仅有接口壳完成归档。

要做什么：基于 08 证据实现 backend 能力接口与实际 adapter，提供 remove-bg/segment/erase 命令及同一 JSON 操作。普通编辑不加载模型；查询支持、配置缺失与不支持参数准确。模型输入/区域/蒙版和输出遵循明确坐标及 alpha，处理超时/取消/错误与进度；复用模型若有实测需要则使用可选 worker。工程记录后端/模型版本、实际参数、seed（若支持）、耗时和真实图像/蒙版结果为必需内容哈希资产；重放不重新调用模型，输入变化不能误用旧输出。

预计修改：pic-core 智能后端/operation/document/pipeline、pic-cli、必要 worker/配置说明/测试、docs/smart-backend 接入说明，本 todo/README。

验收条件：

- [ ] 自动抠图、主体分割、区域对象移除均通过 CLI 和 JSON 真实调用模型，输出可编辑蒙版或实际修复图，边缘/区域外保真有证据。
- [ ] 缺配置/模型、无效尺寸/蒙版、未知参数、后端失败/超时返回清晰非成功状态；普通编辑保持独立工作。
- [ ] 保存工程后断开后端，跨进程预览/undo/redo/重放仍准确恢复；结果资产不能被缓存清理删除。
- [ ] 修改模型输入/参数/区域不复用旧结果，版本冲突/失败不发布部分历史；耗时分项真实。
- [ ] 完整仓库校验及实际模型端到端运行通过；mock 仅用于接口错误回归。

验证：完整仓库校验；真实抠图/分割/修复样例；后端断开恢复、输入变更和失败提交回归。
